# .bashrc
[ -f /etc/bashrc ] && . /etc/bashrc

for f in "$HOME"/.config/shell/*.sh; do
  [ -r "$f" ] && . "$f"
done
unset f

for f in "$HOME"/.config/shell/bash/*.sh; do
  [ -r "$f" ] && . "$f"
done
unset f
