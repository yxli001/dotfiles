autoload -Uz compinit
compinit

[ -r "$HOME/.config/shell/common.sh" ] && source "$HOME/.config/shell/common.sh"

for f in "$HOME"/.config/shell/zsh/*.sh; do
  [ -r "$f" ] && source "$f"
done
unset f

[ -r "$HOME/.config/shell/zsh.local.sh" ] && source "$HOME/.config/shell/zsh.local.sh"
